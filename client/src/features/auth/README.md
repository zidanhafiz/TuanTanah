# features/auth (feature seam — not yet implemented)

Home for the client-side auth UI (login / signup / account), pairing with the
server `modules/auth/`. Nothing here is wired into the router yet — add routes in
`app/App.tsx` when the feature is built. See docs/REFACTORING_PLAN.md Phase 6.
