NODE_ENV=development
PORT=3000

# Supabase (deferred to post-MVP — leave blank to disable)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Redis — leave blank to use in-memory store (local dev without Docker)
REDIS_URL=redis://localhost:6379

# Game config
ROOM_TTL_HOURS=24
MAX_PLAYERS_PER_ROOM=8

# Client (Vite) — URL the browser uses to reach the socket server.
# Leave blank in dev to use the Vite proxy.
VITE_SERVER_URL=
